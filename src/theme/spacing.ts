const spacing = {
    xs: 4,   // tight gaps (icon-label, small text pairs)
    sm: 8,   // pill gaps, nav item gaps, inner padding
    md: 16,  // card padding, section gaps, form field gaps
    lg: 24,  // section spacing, content padding, card groups
    xl: 32,  // major section gaps, tablet content padding
    xxl: 48,  // tablet outer padding, large section breaks
}

/* 
xs - gap between icon + label, vertical text pairs, nav item icon-text
sm - rating pill gaps, category pill gaps, sidebar nav gaps
md - card inner padding, form field spacing, bottom sheet gaps
lg - content area padding (mobile), section gaps, card groups
xl - tablet content gap, two-column gap, major section spacing
xxl - tablet outer padding, large screen margin
*/

export default spacing